<?xml version="1.0" encoding="utf-8"?>
<!--
  FileProvider paths. We only ever expose generated GPX exports written to cacheDir/tracks/ — nothing
  user-private. The authority is ${applicationId}.fileprovider (see AndroidManifest.xml).
-->
<paths>
    <cache-path name="tracks" path="tracks/" />
</paths>
